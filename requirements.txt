# PyTorch CUDA 12.1 wheels (default). For a different CUDA version,
# install torch manually first from https://pytorch.org/get-started/locally/
# and then run `pip install -r requirements.txt` — pip will skip the already-
# satisfied torch line.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0

# HuggingFace stack (MolmoAct2 is loaded via transformers + trust_remote_code)
transformers>=4.52.0
huggingface-hub>=0.24.0

# LeRobot SO-101 drivers
# IMPORTANT: use exactly 0.5.1 — later versions changed the joint-angle
# convention and will cause the arm to slam into the table on startup.
# See https://huggingface.co/docs/lerobot/backwardcomp
lerobot==0.5.1

# Feetech servo SDK — required for the SO-101 motor bus.
# Without it, inference.py silently falls back to a simulated follower
# (the [Follower] Could not connect: No module named 'scservo_sdk' message)
# and the arm will not move.
feetech-servo-sdk

# Cameras
opencv-python>=4.8.0
pyrealsense2>=2.54.0
Pillow>=10.0.0

# Numerics
numpy>=1.24.0
